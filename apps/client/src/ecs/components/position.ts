/**
 * Мировые пиксели в системе Tiled (левый верх карты = 0,0; Y вниз).
 * Центр сущности — точка привязки для визуала и AABB хитбокса (см. Hitbox).
 */
export const Position = {
  x: [] as number[],
  y: [] as number[],
};
